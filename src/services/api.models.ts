export interface IClient {
    id: string;
    name: string;
    email: string;
    role: string;
}

export interface IPolicy {
    id: string;
    name: string;
    email: string;
    role: string;
}
